import { Meta, Story, Description } from '@storybook/addon-docs/blocks';
import { WithGlobalTheme } from '../helpers/decorators';
import doc from './../CHANGELOG.md'

<Story name="Changelog" decorators={[ WithGlobalTheme ]} />

<Meta title="Changelog" parameters={{ viewMode: 'docs' }}/>

# Changelog

Here we keep a log of changes introduces in the library.

<Description markdown={doc}/>
