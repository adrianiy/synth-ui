import { Meta, Story } from '@storybook/addon-docs';
import { WithGlobalTheme } from './../helpers/decorators';

<Story name="Introduction" decorators={[ WithGlobalTheme ]} />

<Meta title="Glyph pipes/Introduction" />

###### COMPOSER
# Introduction

Glyph-core library exports a set of methods to configure your backend easily using a system of **yaml** configuration files.

To do this you can use `composer` and `configureComposer` middlewares.

To know more about them check the **DOCS** 😄 

- [Configure](?path=/docs/glyph-pipes-configure--configure)
- [Composer](?path=/docs/glyph-pipes-composer--composer)
- [Configuration](?path=/docs/glyph-pipes-configuration-yaml--yaml)

### Example

Take a look at the [example YAML](?path=/docs/glyph-pipes-configuration-yaml--yaml#example) to taste what you can do!

