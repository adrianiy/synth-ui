import { html } from 'lit-html';
import { Meta, Description, Canvas, Story } from '@storybook/addon-docs/blocks';
import notes from './readme.md';

export const Template = (args) => {
    return html` <glyph-slider .options=${args.options} @optionChange=${onOptionChange} /> `;
};

<Meta
    title="Components/Slider"
    parameters={{ viewMode: 'docs' }}
/>

# Slider
##### glyph-slider

This component allow users to change between different `options`.

When slider changes a event `onOptionChange` is triggered.

<Canvas>
    <Story name="Slider" args={{ options }}>{Template.bind({})}</Story>
</Canvas>

---

<Description>
    { notes }
</Description>

export const options = [
    { value: 1 },
    { value: 2 },
    { value: 3 }
]

export const onOptionChange = event => alert(event.detail.value);
