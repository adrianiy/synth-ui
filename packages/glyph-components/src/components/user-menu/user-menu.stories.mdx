import { html } from 'lit-html';
import { spread } from '@open-wc/lit-helpers';
import { Meta, Description, Canvas, Story } from '@storybook/addon-docs/blocks';
import notes from './readme.md';

export const Template = args => html`<glyph-user-menu ...=${spread(args)} />`;

<Meta title="Components/User menu" parameters={{ viewMode: 'docs' }} />

# User Menu

##### glyph-user-menu

An user menu that allows users to do logout and modify some application settings like:

- Language
- Theme
- Decimals
- You can show a configuration button to implemt your own configuration modal

## Menu

<Canvas>
    <Story name="Menu" args={{ name: 'Test User', '.customConfig': true, decimals: true, '.themes': themes, '.languages': languages }}>
        {Template.bind({})}
    </Story>
</Canvas>


---

<Description>{notes}</Description>

export const themes =[ {name: 'white', value: 'white', active: true}, { name:'dark', value: 'dark', active: false } ];
export const languages =[ {name: 'english', value: 'en', active: true}, { name:'spanish', value: 'es', active: false } ];
