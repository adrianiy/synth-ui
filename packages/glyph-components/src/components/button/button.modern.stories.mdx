import { html } from 'lit-html';
import { spread } from '@open-wc/lit-helpers';
import { Meta, Description, Canvas, Story } from '@storybook/addon-docs/blocks';
import notes from './readme.md';

export const Template = args => html`<glyph-button ...=${spread(args)} interface='modern' @click=${clickEvent} />`;

<Meta title="Components/Button/Modern" parameters={{ viewMode: 'docs' }} />

###### glyph-button
# Button

Button component, can be rendered with **text** or with **icon**.

## Text button

<Canvas>
    <Story name="Text" args={{ text: 'button1' }}>
        {Template.bind({})}
    </Story>
</Canvas>

## Icon button

<Canvas>
    <Story name="Icon" args={{ icon: 'close' }}>
        {Template.bind({})}
    </Story>
</Canvas>

## Cancel button

<Canvas>
    <Story name="Cancel" args={{ text: 'cancel', cancel: true }}>
        {Template.bind({})}
    </Story>
</Canvas>

## Only text button

<Canvas>
    <Story name="Only text" args={{ text: 'only text', '.onlyText': true }}>
        {Template.bind({})}
    </Story>
</Canvas>

---

<Description>{notes}</Description>

export const clickEvent = () => alert('event')
