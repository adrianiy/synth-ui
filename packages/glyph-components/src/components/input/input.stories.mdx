import { html } from 'lit-html';
import { spread } from '@open-wc/lit-helpers';
import { Meta, Description, Canvas, Story } from '@storybook/addon-docs/blocks';
import notes from './readme.md';

export const Template = args => html`<glyph-input ...=${spread(args)} />`;

<Meta title="Components/Input" parameters={{ viewMode: 'docs' }} />

# Input

##### glyph-input

Handles input rendering.

If autoFocus is `true` the input will be focused on rendering.

## Basic input

<Canvas>
    <Story name="Basic" args={{ placeholder: "Basic input" }}>
        {Template.bind({})}
    </Story>
</Canvas>

## Box input

<Canvas>
    <Story name="Box" args={{ placeholder: "Box input", box: true }}>
        {Template.bind({})}
    </Story>
</Canvas>

## Search input

<Canvas>
    <Story name="Search" args={{ placeholder: "Search input", search: true }}>
        {Template.bind({})}
    </Story>
</Canvas>

## Password input

<Canvas>
    <Story name="Password" args={{ placeholder: "Password input", '.inputType': "password" }}>
        {Template.bind({})}
    </Story>
</Canvas>

## Error input

<Canvas>
    <Story name="Error" args={{ placeholder: "Error input", error: true }}>
        {Template.bind({})}
    </Story>
</Canvas>

## Calendar input

You can configura a minimum and maximum date availables to select with this type of inputs.

In this them we've configured `min` as *2021-01-01* and `max` as *2021-12-31*.

<Canvas>
    <Story name="Calendar" args={{ '.inputType': 'date', '.defaultValue': '2021-02-01', min: '2021-01-01', max: '2021-12-31' }}>
        {Template.bind({})}
    </Story>
</Canvas>

---

<Description>{notes}</Description>

