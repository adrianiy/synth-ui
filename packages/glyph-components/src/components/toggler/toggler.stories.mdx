import { html } from 'lit-html';
import { spread } from '@open-wc/lit-helpers';
import { Meta, Description, Canvas, Story } from '@storybook/addon-docs/blocks';
import notes from './readme.md';

export const Template = args => html`<glyph-toggler ...=${spread(args)} @click=${clickEvent} />`;

<Meta title="Components/Toggler/Classic" parameters={{ viewMode: 'docs' }} />

# Toggler

##### glyph-toggler

Toggler component, it receives an `active` flag that indicates if toggler is active or not.

<Canvas>
    <Story name="Classic" args={{ active: false }}>
        {Template.bind({})}
    </Story>
</Canvas>

---

<Description>{notes}</Description>

export const clickEvent = (event) => {
    console.log('toggle')
}
