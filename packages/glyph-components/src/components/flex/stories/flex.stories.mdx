import { Meta, Description, Canvas, Story } from '@storybook/addon-docs';
import notes from './../readme.md';

<Meta
    title="Components/Flex/Description"
    parameters={{ viewMode: 'docs' }}
/>

###### glyph-flex
# Flex

A helper component to distribute content in your application.

Creates a flex component (with column direction by default) that uses some boolean input options to distribute content inside (see arg. table below).

> **Column** is the default distribution

<Canvas>
    <Story id="components-flex-examples--playground" />
</Canvas>

## Examples

Check more [Examples](?path=/docs/components-flex-examples--playground)

## Properties

<div class="properties">
    <Description>
        {notes}
    </Description>
</div>
