import { html } from 'lit-html';
import { spread } from '@open-wc/lit-helpers';
import { Meta, Description, Canvas, Story } from '@storybook/addon-docs/blocks';
import notes from './readme.md';
export const limit = 3;

export const Template = ({ pages, activePage, limit, pageGroups, onSetPage }) => html`<glyph-pagination pages=${pages} active-page=${activePage} limit=${limit} pageGroups=${pageGroups} @setPage=${changePage} />`;

<Meta
    title="Components/Pagination"
    parameters={{ viewMode: 'docs' }}
/>

###### glyph-pagination
# Pagination

Pagination handler component.

## Basic Pagination

<Canvas>
    <Story
    name="Basic"
    args={{ pages: 4, activePage: 0, onSetPage: changePage }}>{Template.bind({})}</Story>
</Canvas>

## Long pagination

You can configure component to group pages in smaller groups if number of pages goes above some threshold.

<Canvas>
    <Story
    name="Long"
    args={{ pages: 20, activePage: 0, limit: 5, pageGroups: 3, onSetPage: changePage }}>{Template.bind({})}</Story>
</Canvas>

export const changePage = ({ target, detail: page }) => {
console.log(target, page);
    target.activePage = page;
}

